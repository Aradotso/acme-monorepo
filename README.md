# acme-monorepo

## Text formatting utilities

- `capitalize(s: string): string` — `capitalize("hello")` → `"Hello"`
- `truncate(s: string, max: number): string` — `truncate("Hello world", 8)` → `"Hello w…"`
- `slugify(s: string): string` — `slugify("Hello, World!")` → `"hello-world"`
